# Induo UI — contexto del proyecto

Librería de componentes en código para Induo (Aper), construida a partir del design system real en Figma. Este archivo existe para que cualquier sesión de Claude (Code, chat, o quien retome el proyecto) tenga el contexto completo sin tener que volver a explicarlo.

## Origen y fuente de verdad

- El design system vive en Figma: archivo **"UI Kit App Points"** (el mismo archivo aparece en Figma con el nombre extendido "UI Kit - App Points - Pronto Pago - Core", fileKey `4IY8pjPHgMMxxT23IfMzzg` sin cambios), librería de tokens publicada como **"Design Tokens - Induo"** (67 variables: color, spacing, radius, tamaños, stroke — más 10 text styles). **Ojo:** el archivo tiene una SEGUNDA colección de variables con nombres calcados ("UI Kit Pronto Pago", dentro de la librería "UI Kit - App Points - Pronto Pago - Core") — es de otro scope/producto, **no** es la fuente de tokens de este repo. Confirmado con el usuario (2026-07-27): la fuente sigue siendo "Design Tokens - Induo".
- `tokens.json` en la raíz del repo es una copia fiel de esa librería, en formato W3C Design Tokens. Si un token cambia, cambia primero en Figma → se regenera `tokens.json` → se regenera `src/tokens.css`. Nunca al revés.
- `src/tokens.css` es el `@theme` de Tailwind v4, generado a partir de `tokens.json`. **Ojo:** las escalas `--spacing-*`, `--width-*` y `--height-*` usan el valor en píxeles como nombre (`Spacing/8` = `8px`), pisando la escala numérica por defecto de Tailwind (que multiplica x4px). Esto es intencional — `p-8` da `8px`, no los `32px` que daría Tailwind estándar.
- La tipografía original de Figma era Proxima Nova → se migró a Montserrat → **se migró de nuevo a Open Sans (confirmado con el usuario, 2026-07-27)**, ver entrada de log más abajo. Regular 400 y SemiBold 600 siguen siendo los únicos dos pesos del sistema. Se carga vía Google Fonts en `index.html`. Pendiente evaluar self-hostear la fuente para no depender de un tercero.

### Log — retheme de color y tipografía (2026-07-27)

El usuario ajustó la librería de tokens en Figma. Se refetcheó contra el nodo `4067:17219` (Password Input, flujo completo) y varios componentes más (`91:1850` Input, `91:1806`/`91:1820` Checkbox, `6233:2910` RadioButton, `4133:1907` ItemButton, `6055:7029` Toast, `6128:249` CategoryCard, `95:1229` ContextualCard, `8073:14921` ProductCard, `8073:4712` DataCard, `4135:1510` ItemList) usando `get_variable_defs`, confirmado con el usuario vía preguntas antes de tocar nada:

- **`--color-action-primary`: `#8E49CA` (púrpura) → `#2E2BEF` (azul/indigo).** Cambio real e intencional en toda la librería, confirmado por el usuario. Afecta Button primary, foco de Input/PasswordInput/VerificationInput, ItemButton/SpecialButton (ícono+chevron), Badge "action", ContextualCard CTA, ProductCarousel "seeAllLabel", Checkbox/RadioButton checked, etc.
- **`--color-action-primary-pressed`: `#8244B7` → `#221FE4`** (confirmado, nodo `91:1806` Checkbox pressed).
- **`--color-action-secondary`: `#E7DCF0` → `#EAEAFF`**, **`--color-action-secondary-pressed`: `#DBCCE7` → `#E3E3FD`** (confirmados en `8073:14923` FilterBadge y `6124:367` SpecialButton, y re-confirmados directo en `6128:249` CategoryCard durante el sync del 2026-07-27).
- **`--color-information-secondary`: `#D1EDF4` → `#E4F1F4`** (confirmado, nodo `6055:7029` Toast).
- **`--color-success-secondary`: `#D3E7DE` → `#EAF4EF`** (confirmado, nodo `4135:1510` ItemList).
- **`--color-error-secondary`: `#EDD7D7` → `#F2E6E6`** (confirmado, nodo `4135:1610` ItemList error; encontrado en un sync posterior del 2026-07-27, no en la pasada inicial). `--color-warning-secondary` no se pudo confirmar (no se encontró un nodo con esa variable aplicada) — queda con el valor viejo `#F3ECDD`, sin tocar, hasta poder confirmarlo contra Figma.
- **`--color-action-primary-hover`: `#9660C5` → `#3F3CEE`** y **`--color-action-secondary-hover`: `#EAE0F4` → `#ECECFF`** — confirmados en una segunda pasada contra el nodo real del Button (`18:4` canvas "↳Button", variantes `18:203`/`18:225` Property 1=Hover de "Big Primary Out"/"Big Secondary Out"). CategoryCard y SpecialButton no tienen una variante Hover propia en Figma (son symbols de un solo frame) pero comparten las mismas variables de Button, así que este valor aplica igual.
- **Tipografía: `Montserrat` → `Open Sans`** en los 10 text styles, confirmado por el usuario. **Inconsistencia encontrada en el propio archivo de Figma:** el text style "Body/Small Semibold" resuelve a `Open Sans` en unos nodos (`6128:249` CategoryCard) pero todavía a `Montserrat` en otros (`6055:7029` Toast, `8073:4712` DataCard) — parecen frames que no se resuscribieron al estilo actualizado. Se tomó `Open Sans` como valor correcto en todo el kit (coincide con la confirmación del usuario y con la mayoría de las muestras), pero si en algún componente el texto semibold pequeño se ve con la fuente vieja, es este bug de Figma, no un error de nuestro lado.
- Verificado: `--color-error-primary`, `--color-warning-primary`, `--color-success-primary`, `--color-information-primary` y toda la escala de grises/neutros **no cambiaron**.

### Log — segundo ajuste de `action-*` (2026-07-27, sync posterior)

Al correr el sync de nuevo (mismo día), Paso 1 encontró que **toda la familia `action-*` había cambiado otra vez** en Figma desde la pasada anterior — confirmado en 4 nodos distintos (`18:149`/`18:221`/`18:225` Button, `8089:14648` PasswordInput, `6128:249` CategoryCard) vía `get_variable_defs`, sin necesidad de preguntarle al usuario esta vez porque ya está establecido que la fuente de verdad es Figma y el propio `SYNC.md` autoriza aplicar diferencias reales sin confirmación previa:

- **`--color-action-primary`: `#2E2BEF` → `#932BEF`** (pasó de azul/indigo a púrpura/violeta — no es el mismo púrpura original `#8E49CA` de antes del primer retheme, es un tono nuevo).
- **`--color-action-primary-hover`: `#3F3CEE` → `#993BED`**.
- **`--color-action-primary-pressed`: `#221FE4` → `#8520DE`**.
- **`--color-action-secondary`: `#EAEAFF` → `#E8DFF0`**.
- **`--color-action-secondary-hover`: `#ECECFF` → `#EFE9F4`**.
- **`--color-action-secondary-pressed`: `#E3E3FD` → `#E1D5EC`**.
- Verificado que **no cambió nada más** en esta pasada: `error-primary`/`error-secondary`, `success-primary`/`success-secondary`, `warning-primary`, escala de grises, todos estables en los valores del retheme anterior. `--color-warning-secondary` sigue sin poder confirmarse.
- Verificado en vivo en browser: `backgroundColor` del Button primary → `rgb(147, 43, 239)` = `#932BEF`.

## Stack

React + TypeScript + Vite + Tailwind CSS v4. Sin librería de componentes de terceros (no shadcn, no MUI). Cada componente es un `.tsx` propio en `src/components/<Nombre>/<Nombre>.tsx`, exportado desde `src/components/index.ts`.

## Convenciones establecidas

- **Los estados de Figma (Hover/Pressed/Active/Disabled) se traducen a pseudo-clases CSS nativas** (`hover:`, `active:`, `focus:`, `disabled:`), nunca a props explícitas tipo `state="hover"`. El navegador maneja el estado, no un prop.
- **Los inputs con valor/placeholder usan `::placeholder` para diferenciar estado vacío vs. con contenido**, no JS ni estado de React, cuando es posible resolverlo en CSS puro.
- Componentes con variantes visuales (Button, Badge) usan un objeto `Record<Variant, string>` de clases, no librerías como `class-variance-authority` — se mantiene deliberadamente simple.
- Cada componente exporta su tipo de Props (`export interface XProps`) y se re-exporta desde `src/components/index.ts`.
- Los íconos son SVG propios en `src/components/icons/`, `currentColor` en el stroke para heredar color por contexto.
- **Tipografía: usar las clases `text-body-*`/`text-heading-*`, nunca `text-[Npx] leading-[Mpx]` a mano.** `tokens.css` define `--text-body-large: 18px` + `--text-body-large--line-height: 24px` (mismo patrón para medium/small y sus `-semibold`, y para heading-large/medium/small/extra-small) dentro de `@theme`, y Tailwind v4 genera automáticamente la clase `text-body-large` aplicando font-size Y line-height juntos — confirmado compilando y mirando el CSS generado. El peso (`font-normal`/`font-semibold`) sigue siendo una clase aparte, el nombre del token (`-semibold`) es solo documentación. Cuando Figma usa un line-height que NO es el del token (pasa en ContextualCard, DataCard, PasswordInput — están comentados en el código), usar `text-[length:var(--text-body-large)]` (o el token de tamaño que corresponda) + un `leading-[Npx]` literal, para al menos mantener el tamaño sincronizado al token.
- Antes de construir un componente nuevo, traer el diseño real de Figma (no inventar medidas/colores) — si tenés el MCP de Figma conectado en esta sesión de Claude Code, usalo contra el archivo "UI Kit App Points" (fileKey: `4IY8pjPHgMMxxT23IfMzzg`). Si no lo tenés conectado, pedir referencia visual antes de adivinar.
- **Button, Badge y Toast no cambian** — son correctos como están, construidos a mano. **ItemButton, ItemCard e Input SÍ cambiaron en Figma después de construidos** (ver sus entradas abajo) — antes de tocarlos, refetchear contra los nodos documentados para confirmar que el código sigue vigente.
- **Para componentes del backlog con comportamiento no trivial (foco, teclado, ARIA, posicionamiento)** — Tooltip, Select/Dropdown, Modal, Radio Button en grupo, y cualquier otro con ese perfil — construir sobre **Radix UI primitives** (`@radix-ui/react-*`) en vez de implementar el comportamiento desde cero. Radix no trae estilos propios, solo comportamiento y accesibilidad ya resueltos; el styling sigue siendo 100% con las clases de Tailwind y los tokens de este proyecto, igual que en el resto de los componentes. No traer shadcn/ui completo ni su CLI — solo los primitives de Radix que hagan falta, instalados uno por uno.

## Estado actual — componentes construidos

- ✅ **Button** — variantes `primary` / `secondary` / `outlined` / `text`, tamaños medium/small.
- ✅ **Input** — estados Default/Hover/Active/Complete/Error, con `helperText` y `errorMessage`. Alto `h-[var(--height-56)]` (56px) — antes tenía `h-[58px]` hardcodeado, un valor que no correspondía a ningún token; el frame de Figma (`91:1850`) referencia explícitamente el token `--width-and-height-56`. **Bug de precedencia hover/focus confirmado y corregido (2026-07-27):** tenía el mismo patrón que se detectó en PasswordInput (`hover:border-gray-5` + `focus:border-2 border-action-primary`, misma especificidad, Tailwind v4 hace ganar siempre a `hover:`). Fix: `not-focus:hover:border-...`. Verificado en vivo (click + `document.activeElement`/`:hover` simultáneos → `borderColor` confirmado `rgb(142, 73, 202)` = `--color-action-primary`).
- ✅ **Badge** — variantes semánticas `information` / `success` / `warning` / `error` / `action`, tamaños regular/small.
- ✅ **ItemButton** — fila ícono (32px, `PinLocationIcon`, color `action-primary`) + label (+ subtitle opcional) + chevron (`ChevronRightThinIcon`, también `action-primary`). Fondo blanco por defecto, `gray-2` en hover, `gray-1` en active — sin borde. **Historial de cambios en Figma** (ya van 3 rediseños desde que se construyó): pin genérico gris → `PinLocationIcon` púrpura; flecha larga → chevron fino; fondo `gray-1` fijo → blanco/gray-2/gray-1. Antes de asumir que el código vigente coincide con Figma, refetchear `4133:1907`/`2520`/`2598` (Default/Hover/Pressed).
- ✅ **ItemCard** — mismo patrón que ItemButton, misma paleta blanco/gray-2/gray-1 (ya NO tiene el modelo de fondo púrpura ni de borde que tuvo en versiones anteriores). Prop `selected` fija el fondo en `gray-1` de forma persistente (sin depender de hover) — Figma no muestra ya un estado "Selected" distintivo, así que esta prop es una interpretación razonable, no algo pixel-perfect documentado. Pensado para grupos de opciones (método de envío, medio de pago, etc.). Refetchear `8073:14879`/`14881`/`14901` (Default/Hover/Pressed) antes de asumir vigencia.
- ✅ **Toast** — variantes `default` / `success` / `warning` / `error` (bg y border del mismo color, texto blanco), ícono (32px) + mensaje + botón de acción transparente opcional. Fetcheado de Figma (node `6055:7029`); ahí también hay una variante `success` no mencionada originalmente en este archivo, se incluyó igual porque comparte estructura y colores semánticos con el resto del kit (Badge ya usa success/warning/error/action). Íconos: `InterfaceEssentialWarningCircleIcon` (32px, círculo+exclamación, default/warning/error) y `ToastSuccessIcon` (25px, success) — ambos generados por `scripts/convert-icons.mjs` desde SVG reales de Figma. **Ojo con la confusión de nombres:** Figma llama "Interface, Essential/Warning" tanto a este ícono circular de 32px como al hexágono de 16px que usan Input/PasswordInput/VerificationInput (`WarningIcon`) — son dos íconos distintos con el mismo nombre de capa; en una revisión anterior se usó por error el hexágono acá, ya corregido.
- ✅ **CategoryCard** — botón fijo 100x124px, ícono (40px) + label (14px semibold, 2 líneas). Fondo `action-secondary`, estados hover/pressed vía pseudo-clases. Ícono nuevo `OutdoorIcon` en `src/components/icons/`. Dimensiones y tipografía ajustadas más de una vez en Figma después de construido — revisar contra el nodo (`6128:203`) si hay dudas de si el código sigue vigente.
- ✅ **Tooltip** — primer componente sobre Radix (`@radix-ui/react-tooltip`). Bubble oscura (`gray-9`), ícono (24px, `InfoCircleIcon`) + mensaje (16px/20px), botón de cerrar opcional (`onClose`, `CloseIcon` nuevo) y link de acción opcional subrayado (`actionLabel`/`onAction`). Cada instancia envuelve su propio `Tooltip.Provider` (no hace falta uno global a nivel app). Fetcheado de Figma (`4067:6827` variante simple, `4067:6610` variante con acción). Requirió convertir `Button` a `forwardRef` (ver abajo) para que funcione como trigger vía `asChild`. **Pendiente:** `InfoCircleIcon` se inventó sin ver el ícono real — ya existe `InterfaceEssentialInfoCircleIcon` (generado desde Figma) para reemplazarlo cuando se retome.

- ✅ **Separator** — `<hr>` semántico, 1px `gray-3`. Fetcheado de Figma (`4133:1584`).
- ✅ **Checkbox** — `<input type="checkbox">` nativo (oculto, `peer`) + spans estilados con `peer-checked:`/`peer-hover:`/`peer-disabled:`, mismo patrón que `Input`. Ícono `CheckboxCheckIcon` nuevo (path real de Figma, no inventado). Fetcheado de Figma (`91:1820` default, `93:906` hover, `91:1806` pressed/checked, `91:1800` selected, `91:1810` disabled).
- ✅ **RadioButton** — mismo patrón que Checkbox (`input type="radio"` nativo + spans `peer-*`), círculo + dot interno vía CSS puro (sin ícono, son solo `border-radius-full`). **No usa Radix** pese a la convención de abajo: al ser un `<input type="radio">` real (solo estilado, no reemplazado), el navegador ya resuelve foco/teclado/ARIA/agrupación por `name` sin reimplementar nada — Radix tiene sentido cuando no hay un input nativo debajo, acá sí lo hay. Fetcheado de Figma (`6233:2910` default, `6233:2923` hover, `6233:2912` selected, `6233:2921` disabled, `6233:2918` selected+disabled).
- ✅ **VerificationInput** — fila de casilleros (`length`, default 4), cada uno un `<input maxLength={1}>` real (48x58, border-1 gray-4 default, border-2 action-primary si tiene valor o está en foco, border-2 error-primary si `errorMessage`). Auto-advance de foco al siguiente casillero al tipear, backspace en uno vacío borra y vuelve al anterior, soporta pegar el código completo. Value/onChange es un string único (no un array), igual que el resto de los inputs del kit. Sin Radix — mismo razonamiento que RadioButton, son inputs nativos reales, el manejo de foco entre casilleros es lógica de la app, no algo que Radix resuelva. Fetcheado de Figma (`4058:2597` default, `4058:2621` active, `4058:2629` complete, `4058:2613` error). **Mismo bug de precedencia hover/focus que Input/PasswordInput, confirmado y corregido (2026-07-27):** casillero vacío tenía `hover:border-gray-5` + `focus:border-2 border-action-primary` a igual especificidad — fix `not-focus:hover:border-...`, verificado en vivo igual que Input.
- ✅ **SpecialButton** — mismo patrón que ItemButton pero con la paleta de CategoryCard: bg/border/texto en tonos de `action-secondary`/`action-primary`, ícono también teñido de púrpura (28px dentro del box de 32px). Reusa `ChevronRightThinIcon`. Fetcheado de Figma (`6124:367`).
- ✅ **FilterBadge** — chip removible: label (16px Regular `action-primary`) + botón "X" opcional (`onRemove`, ícono nuevo `FilterBadgeCloseIcon`). Si no se pasa `onRemove`, se muestra sin el botón de cerrar. Fetcheado de Figma (`8073:14923`).
- ✅ **DataCard** — campo de datos de solo lectura: label (14px) + value (18px) + CTA "Editar" opcional (`onEdit`, ícono nuevo `InterfaceEssentialPenEditIcon`). Ancho fijo 340px, igual que Input/VerificationInput/ItemButton. Fetcheado de Figma (`8073:4712`).

## Pipeline de íconos (`scripts/convert-icons.mjs`)
Los SVG crudos exportados de Figma van en `src/icons-raw/*.svg`. El script toma el primer bloque `<svg>` de cada uno, saca `width`/`height`/`style`/`preserveAspectRatio`/`overflow`, pasa `stroke`/`fill` en hex **o en nombres literales (`white`/`black`)** a `currentColor`, convierte atributos kebab-case a camelCase, agrega `{...props}` y genera el componente en `src/components/icons/<PascalCase-del-archivo>Icon.tsx`. Correrlo de nuevo es seguro (sobreescribe). Íconos generados y sin usar todavía (para Special Button, y para reemplazar el de Tooltip): `InterfaceEssentialInfoCircleIcon`, `InterfaceEssentialSelectObjectIcon`, `PaymentsFinanceInvoiceList3Icon`, `PaymentsFinanceDollarIcon`.

## Convención agregada: `Button` usa `forwardRef`
Radix (`Tooltip.Trigger`, y a futuro `Select`/`Popover`/etc.) usa `asChild` + clonado para inyectar su propio ref en el elemento trigger. Si el componente pasado como hijo no reenvía el ref con `React.forwardRef`, Radix no puede calcular la posición del contenido y React tira warning en consola. `Button` ya se convirtió; **cualquier componente que pueda usarse como trigger de un primitive de Radix debe reenviar ref de la misma forma**.

- ✅ **ContextualCard** — bg `gray-1`, radius medium, mensaje (18px Regular, color `neutral-black` literal, `leading-22px` — no es el body-large estándar de 24px, es un override puntual de Figma) + CTA de texto opcional (`actionLabel`/`onAction`, 18px SemiBold `action-primary`). Fetcheado de Figma (`95:1229`).
- ✅ **ProductCard** — card fija 160px de ancho, imagen 160x160 (bordeada, radius-large arriba) + bloque de info 183px alto (radius-large abajo, mismo borde `gray-4` sin duplicar el borde compartido). Props: `image`, `badge?` (ReactNode, ej. `<Badge variant="information" size="small">`), `brand`, `title` (2 líneas, `line-clamp-2`), `price`, `shipping?`. El `badge` va en el grupo de abajo, justo arriba de `price` (no en el grupo brand/title) — Figma lo movió ahí después de construido. Fetcheado de Figma (`8073:14921`, antes `6102:231` — el node-id cambió).
- ✅ **PasswordInput** — componente controlado (`value`/`onChange`, no maneja su propio estado de texto). Input `h-[var(--height-56)]` (56px, igual que `Input` — antes tenía `h-[52px]` hardcodeado, corregido 2026-07-27, ver log más abajo) con toggle mostrar/ocultar. **Íconos invertidos respecto a la intuición común:** el ojo **tachado** (`InterfaceEssentialEyeHideIcon`) se muestra cuando la contraseña está **oculta** (masked, `type="password"`), y el ojo **abierto** (`InterfaceEssentialEyeShowIcon`) cuando está **visible** (`type="text"`) — así lo muestra Figma en los 5 estados (Default/Hover/Active/Complete/Error), no al revés. Borde: `gray-4` default, `gray-5` en hover o cuando el input tiene valor (`hasValue`, derivado de `props.value`, ver el componente — no hay pseudo-clase CSS para "tiene valor"), `border-2 action-primary` en foco (`focus-within:` en el wrapper, ya que el borde vive en el div contenedor, no en el `<input>`), `border-2 error-primary` con `errorMessage`. Panel opcional de requisitos (`requirements: {label, met}[]`) con barra de fortaleza de 2 segmentos (`strength: "weak"|"medium"|"strong"` → fill fijo 15%/56%/71% y color error/warning/success, valores tomados literal de los 3 mockups de Figma, no es una fórmula continua) y checklist con ícono check/cross (`InterfaceEssentialCheckThinIcon`/`InterfaceEssentialCrossThinIcon`) según `met`. `errorMessage` opcional (mensaje con `WarningIcon`). El componente NO calcula fortaleza ni si los requisitos se cumplen — eso lo decide quien lo consume. Fetcheado de Figma (`8089:14647` default, `8089:14649` hover, `8089:14699` active, `8089:14716` complete, `8089:14733` error — reemplaza los nodos viejos `4069:4084`/`4067:17334`/`4069:4960`, cambiaron de id).
  - **Revisión de comportamiento (2026-07-27, contra el flujo completo documentado en Figma node `4067:17219`):** se re-fetcheó el spec completo (5 estados + Accepted/Complete/Medium/Not accepted/Error) y se encontró un bug real de CSS, no solo visual: `hover:border-gray-5` y `focus-within:border-2 border-action-primary` tienen la MISMA especificidad CSS (una clase + una pseudo-clase cada una), y Tailwind v4 siempre emite el bloque `@media (hover:hover)` después de las demás variantes en la hoja generada — así que ante un empate de especificidad, `hover:` gana siempre. Como el mouse queda quieto sobre el input justo después de hacer click para escribir, el borde volvía a gris en vez de quedar violeta. Confirmado inspeccionando `document.styleSheets` en vivo (no era una sospecha, se vio el orden real de las reglas). **Fix:** cambiar `hover:border-...` por `not-focus-within:hover:border-...` (variante `not-*` de Tailwind v4) — así el hover solo aplica cuando el wrapper NO está en foco, sin necesidad de reordenar nada. **Auditoría posterior (2026-07-27):** se grepeó todo `src/components/` buscando el mismo patrón (`hover:border` + `focus:`/`focus-within:border` sobre la misma propiedad). Encontrado también en `Input` y `VerificationInput` (ver sus entradas arriba), corregido igual con `not-focus:hover:`. `RadioButton`/`Checkbox` usan `peer-hover:` pero combinado con `peer-checked:` (no `peer-focus:`), es un estado distinto — no tienen la carrera. No quedan casos pendientes de este patrón en el kit.
  - **Glyph de máscara — decisión deliberada de NO seguir a Figma al pie de la letra:** Figma especifica el carácter de máscara como texto literal `⬤ ⬤ ⬤ ⬤ ⬤ ⬤ ⬤ ⬤` (círculos grandes separados por espacio, no el bullet nativo del navegador). Se evaluó reproducirlo pero se descartó a propósito: hacerlo requeriría reemplazar el `<input type="password">` real por un input de texto enmascarado con una capa decorativa superpuesta (rompe autofill/gestor de contraseñas, copiar/pegar, y lectura por lectores de pantalla) o usar `-webkit-text-security` (no soportado en Firefox — si se aplicara sobre un `type="text"`, un usuario de Firefox vería la contraseña en texto plano, una regresión de seguridad real). Se prioriza el `<input type="password">` nativo con su bullet propio del sistema operativo/navegador sobre el pixel-perfect del glyph de Figma — esto es intencional, no un pendiente.
  - **Accesibilidad agregada:** `aria-describedby` en el `<input>` apuntando al `id` del mensaje de error (mismo patrón que `Input`, antes faltaba acá) y `aria-pressed={visible}` en el botón de mostrar/ocultar (es un toggle, no una acción simple).
  - **Revisados y confirmados sin cambios:** tipografía del panel de requisitos (los overrides `16px/18px` del título y `14px/20px` del helper/checklist coinciden exactamente con el spec de Figma, re-confirmado contra `4067:17334`).
  - **Corrección real (2026-07-27, reportado por el usuario contra `86:3810`/`4067:17219`): el componente estaba desalineado de `Input` en varios puntos, no solo el borde.** Se refetcheó `8089:14648` (los 5 estados juntos) con `get_design_context` completo (no solo `get_variable_defs`) y se encontraron 4 bugs reales:
    1. **Tamaño de fuente del input y del placeholder:** estaba en `text-body-small` (14px/18px), Figma usa `text-[18px]` (Body/Large) — el mismo tamaño que `Input`. Fix: `text-body-large`.
    2. **Color del placeholder:** no tenía ningún `placeholder:` — dependía del color por default del navegador. Figma define el placeholder (estados Default/Hover) en `gray-5`. Fix: `placeholder:text-[var(--color-neutral-gray-5)]`. El color del texto tipeado (`neutral-black`, no `gray-9` como en `Input`) sí es correcto tal cual estaba — es una diferencia real entre los dos componentes en el propio Figma, no un bug.
    3. **Tamaño de los íconos de mostrar/ocultar contraseña — la nota anterior de esta entrada estaba mal.** Se había documentado como "proporciones nativas correctas de cada asset" (`19x24` para el ojo abierto vs `24x24` para el tachado) pero el nodo real de Figma (`8089:14648`) muestra el contenedor del ícono como `size-[24px]` cuadrado en los 5 estados, sin excepción — la diferencia de tamaño entre los dos íconos en el código era un bug, no una decisión de diseño. Fix: los dos íconos ahora renderizan a `size-[24px]`, aceptando una leve distorsión de aspecto en el ojo abierto (su viewBox nativo es `18.8995x14.9`, no cuadrado) porque así lo define Figma.
    4. **El panel de requisitos aparecía siempre que se pasaba la prop `requirements`, sin importar el foco.** Según la documentación del flujo completo (`4067:17219`): el panel tiene que aparecer mientras el usuario está escribiendo (estado Active, con los 3 sub-casos Not accepted/Medium/Accepted) y desaparecer en los estados de reposo (Complete y Error, que en Figma solo ocurren después de que el usuario "deja de editar"). Se agregó estado interno `isFocused` (antes no existía ningún tracking de foco) y el panel ahora solo se renderiza con `isFocused && !hasError && requirements?.length`. El tracking de foco/blur va en el div wrapper completo (con `event.relatedTarget` + `wrapperRef.current.contains(...)`), no en el `<input>` solo — si se pone únicamente en el input, tocar el botón de mostrar/ocultar contraseña (que también puede quedarse con el foco) cierra el panel a mitad de tipeo, un bug real que apareció durante la implementación y se verificó en vivo antes de darlo por bueno.
    - Verificado en vivo en browser: foco → aparece el panel; click en el botón del ojo → panel se mantiene abierto y el input sigue enfocado; click afuera → panel se cierra y el input con `errorMessage` nunca muestra el panel aunque se lo enfoque.
- ✅ **ProductCarousel** — header (título 18px SemiBold + `seeAllLabel` opcional, renderizado con `<Button variant="text" icon={null}>` — el "Big Text" del kit, no un `<button>` a mano) + fila horizontal scrolleable de `ProductCard` (gap 4px, no 16 — ojo que es distinto al resto del kit; scrollbar nativo oculto vía `scrollbar-width:none` + `::-webkit-scrollbar`). Los 4 dots de paginación son reales: trackean `scrollLeft/(scrollWidth-clientWidth)` en `onScroll` y resaltan el dot más cercano (no es "un dot por card", es una barra de progreso de 4 pasos fija, igual que el mock de Figma). Fetcheado de Figma (`6104:4022` header, reusa `6102:289` para las cards).
- ✅ **ItemList** — fila de historial/movimientos, NO interactiva (`<div>`, no `<button>`): ícono (32px, bg semántico) + título/monto en la fila de arriba, status label/fecha en la de abajo. Prop `variant` (`information`/`success`/`warning`/`error`, mismo set semántico que Badge) define el color del bg del ícono y del status label — el ícono en sí se pasa como prop, el componente no lo elige. Íconos nuevos usados en el demo: `InterfaceEssentialCheckmarkCircleIcon`, `InterfaceEssentialStarFavoriteAddIcon`, `InterfaceEssentialDeleteCrossHexagonIcon`, `InterfaceEssentialBackwardBackIcon`. Fetcheado de Figma (`4135:1510` success, `4135:1558` information/estrella, `4135:1610` error, `4135:1662` information/devolución).

## Pendiente / backlog
- Ninguno — todos los componentes moleculares del kit original (Button, Input, Badge, ItemButton, ItemCard, Toast, CategoryCard, Tooltip, ContextualCard, ProductCard, ProductCarousel, PasswordInput, Separator, Checkbox, RadioButton, VerificationInput, SpecialButton, FilterBadge, DataCard, ItemList) están construidos. Si aparecen componentes nuevos en Figma, se agregan acá.
- Evaluar self-hostear Montserrat (`@fontsource/montserrat` o similar) en vez de depender de Google Fonts.
- El archivo Figma "UI Kit Payment Web" (fileKey `2fH1HSTQNFS2AKBnu42sTY`) todavía no está conectado a la librería "Design Tokens - Induo" — quedó pendiente, no es prioridad actual.

## Deploy

- Repo: `github.com/jes-ux/induo-ui`, rama `main`.
- Cada push a `main` dispara un redeploy automático en Vercel: `induo-ui.vercel.app`.
- `npm run dev` para desarrollo local, `npm run build` para verificar que compila antes de pushear.
